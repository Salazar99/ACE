ASSUME:
  rst_n ∈ {0,1}
  start ∈ {0,1}

GUARANTEE:
  G(start = 1 -> F(done = 1))
  G(done = 1 -> G(yout = yout_final))
  G(start = 1 ∧ ¬done -> X(yout > yout_prev))


ain_init, bin_init yout_prev, yout_final do not exist



      